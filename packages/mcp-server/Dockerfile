# Generated by Lobbi
FROM node:18-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install --production

COPY index.js ./

# The MCP protocol typically uses stdio, so no EXPOSE is strictly needed
# but we can add one if it supports SSE. This one seems stdio-based.
CMD ["node", "index.js"]
